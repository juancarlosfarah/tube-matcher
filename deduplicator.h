#ifndef DEDUPLICATOR_H
#define DEDUPLICATOR_H
	
	/* function Deduplicate
 	 * ====================
 	 * this function takes a string, removes any
 	 * duplicate characters it may have and returns
 	 * the string without those duplicates.
 	 */ 
	std::string Deduplicate(std::string); 

#endif
